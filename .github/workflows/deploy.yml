name: Build and deploy backe-end with Docker
run-name: ${{ github.actor }} is building a new DockerFile on push
on:
  push:
    branches:
      - 15-deploy-back-end-server-on-aws-ec2-instance-using-docker


jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
#      - name: Check out the repo
#        uses: actions/checkout@v4

#      - name: Log in to Docker Hub
#        uses: docker/login-action@f4ef78c080cd8ba55a85445d5b36e214a81df20a
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}

#      - name: Extract metadata (tags, labels) for Docker
#        id: meta
#        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
#        with:
#          images: nkuusik/django-paralleltext

# Siin tee run käsuga ümber
#      - run: cd back/paralleltext/
#      - run: echo $(pwd)
#      - run: docker build back/paralleltext/. -t nkuusik/django-paralleltext
#      - run: docker push nkuusik/django-paralleltext


      - uses: easingthemes/ssh-deploy@v5.0.0
        name: Deploy over SSH
        with:
          SSH_PRIVATE_KEY: ${{ secrets.AWS_KEY }}
          REMOTE_HOST: ${{ vars.AWS_HOST }}
          REMOTE_USER: ${{ vars.AWS_USER }}
          SCRIPT_AFTER: |
            ./script
            echo $(pwd)
